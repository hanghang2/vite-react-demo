import styles from '../index.module.scss';

const Header = () => {
    return (
        <div className={`${styles.header} d-flex justify-content-end p-x-30 header-btns`}>
        </div>
    );
};

export default Header;
